{
  "name": "headcount",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A satirical 1990s office simulator. Beige, fluorescent, slightly broken.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^7.0.0",
    "vitest": "^4.1.10"
  }
}
